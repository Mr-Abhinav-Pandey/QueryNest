# QueryNest Frontend

Flutter client for QueryNest.

For project overview, setup instructions, and API documentation, see the [root README](../README.md).

## Quick start

```bash
flutter pub get
flutter run -d chrome
```

Ensure the backend is running at `http://localhost:8000` before using the app.
